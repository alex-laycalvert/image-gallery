<nav class="navbar navbar-expand-lg navbar-light" id="site-navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="/gallery"><h4>Image Gallery</h4></a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-lg-0">
                <% if (isAuthenticated) { %>
                    <li class="nav-item dropdown">
                        <% if (page == 'gallery' || page == 'user_gallery') { %>
                            <a class="nav-link active dropdown-toggle" href="/gallery" id="galleryDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <% } else { %>
                            <a class="nav-link dropdown-toggle" href="/gallery" id="galleryDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <% } %>
                        Gallery
                        </a>
                        <div class="dropdown-menu" aria-labelledby="galleryDropdown">
                            <a href="/gallery" class="dropdown-item">Public Gallery</a>
                            <a href="/user/gallery" class="dropdown-item">Your Gallery</a>
                        </div>
                    </li>
                <% } else { %>
                <li class="nav-item">
                    <% if (page == 'gallery') { %>
                        <a href="/gallery" class="nav-link active">
                    <% } else { %>
                        <a href="/gallery" class="nav-link">
                    <% } %>
                    Gallery
                    </a> 
                </li>
                <% } %>
                <li class="nav-item">
                    <% if (page == 'upload') { %>
                    <a class="nav-link active" href="/upload">
                        Upload an Image
                    </a> 
                    <% } else { %> 
                    <a class="nav-link" href="/upload">Upload an Image</a> 
                    <% } %>
                </li>
            </ul>

            <ul class="navbar-nav mx-4 mb-lg-0">
                <% if (isAuthenticated) { %>
                    <li class="nav-item dropdown">
                        <% if (page == 'user_account') { %>
                            <a class="nav-link active" href="/user/account" id="accountDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <% } else { %>
                            <a class="nav-link" href="/user/account" id="accountDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <% } %>
                        <h5><i class="fa fa-user-circle-o"></i> <%= user.username %></h5></a>
                        <div class="dropdown-menu" aria-labelledby="accountDropdown">
                            <% if (user.role == 'ADMIN') { %>
                                <a href="/user/admin" class="dropdown-item">Admin Dashboard</a>
                            <% } %>
                            <a href="/user/account" class="dropdown-item">Your Account</a>
                            <a href="/user/gallery" class="dropdown-item">Your Gallery</a>
                            <div class="dropdown-divider"></div>
                            <form action="/user/logout?_method=DELETE" method="POST">
                                <input class="dropdown-item" type="submit" value="Logout">
                            </form>
                        </div>
                    </li>
                <% } else { %>
                <li class="nav-item">
                    <% if (page == 'user_login' || page == 'user_signup') { %>
                        <a href="/user/login" class="nav-link active">
                    <% } else { %>
                        <a href="/user/login" class="nav-link">
                    <% } %>
                    <h5><i class="fa fa-user-circle-o"></i> Log In</h5></a> 
                </li>
                <% } %>
            </ul>
            <form action="/gallery" class="d-flex" method="GET">
                <input
                    class="form-control me-2"
                    type="search"
                    placeholder="Search"
                    name="search"
                    aria-label="Search"
                    value="<%= search %>"
                />
                <button class="btn btn-outline-dark" type="submit">
                    <i class="fa fa-search"></i>
                </button>
            </form>
        </div>
    </div>
</nav>
<div class="dropdown-divider"></div>
